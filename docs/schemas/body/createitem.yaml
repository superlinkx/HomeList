description: Item body to be created
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        content:
          type: string
        sort:
          type: integer
          format: int64
      required:
        - content
        - sort
