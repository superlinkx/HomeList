description: Item body to be updated
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        content:
          type: string
        sort:
          type: integer
          format: int64
        checked:
          type: boolean
